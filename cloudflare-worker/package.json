{
  "name": "gemini-firebase-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Gemini API with Firebase Auth",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.2.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
